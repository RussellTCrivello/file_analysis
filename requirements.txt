# Core runtime requirements (regenerated from actual imports - Phase 14).
# Optional format support lives in setup.py extras: [pdf] [office] [ocr] etc.

# --- Web framework ---
Flask>=3.0,<4
flask-babel>=4.0
flask-limiter>=3.5
flask-wtf>=1.2
flask-compress>=1.15

# --- Database ---
psycopg2-binary>=2.9

# --- Utilities ---
python-dateutil>=2.8
python-dotenv>=1.0
psutil>=5.9
chardet>=5.0
PyYAML>=6.0

# --- Text extraction (core readers) ---
html2text>=2024.2.26

# --- Email reader ---
extract-msg>=0.47

# --- Archive readers ---
rarfile>=4.2
py7zr>=0.21

# --- Calendar ---
icalendar>=5.0

# Removed from the legacy manifest (audit Phase 14):
# - `logging`  (malicious/obsolete PyPI package shadowing the stdlib)
# - pdfplumber (unused; PyMuPDF is the authoritative PDF implementation)
# - moviepy, opencv-python (heavy; moved to [media] extra - video reader
#   degrades gracefully without them)
