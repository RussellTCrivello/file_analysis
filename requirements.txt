# File Analysis - complete Python runtime dependency manifest
# Install the complete reader set with:
#   python -m pip install -r requirements.txt
#
# Some readers also require native programs. See the notes at the bottom.
# Python >= 3.10 is required.

# Web application and API
Flask>=3.0,<4
flask-babel>=4.0
flask-limiter>=3.5
flask-wtf>=1.2
flask-compress>=1.15

# Database, configuration, monitoring
psycopg2-binary>=2.9
python-dateutil>=2.8
python-dotenv>=1.0
psutil>=5.9
PyYAML>=6.0
chardet>=5.0

# PDF and document readers
PyMuPDF>=1.24
PyPDF2>=3.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
xlrd>=2.0
odfpy>=1.4
striprtf>=0.0.26
docx2txt>=0.8

# Images and OCR
Pillow>=10.0
pytesseract>=0.3.10
rapidocr-onnxruntime>=1.4
opencv-python-headless>=4.9

# Audio and video
mutagen>=1.47
moviepy>=1.0.3

# Archives and ebooks
rarfile>=4.2
py7zr>=0.21
ebooklib>=0.18
beautifulsoup4>=4.12
mobi>=0.3

# Email and calendar
extract-msg>=0.47
html2text>=2024.2.26
icalendar>=5.0
# libpff-python provides the pypff module used for PST files. It may need
# platform-specific build tooling and may not publish wheels for every Python
# version/OS. If pip cannot install it, PST files will be metadata-only.
libpff-python

# Development and test tooling (safe to keep in a development environment)
pytest>=8.0
pytest-cov>=4.1
ruff>=0.4
bandit>=1.7
pgserver>=0.1

# Native/system dependencies that pip cannot install:
# - Tesseract OCR executable: required by pytesseract when RapidOCR is not used.
# - UnRAR or bsdtar: required by rarfile for RAR extraction.
# - PostgreSQL server: required by the application database.
# - FFmpeg: required by moviepy/video processing and some audio/video formats.
# - libpff/libpst build dependencies may be required to build libpff-python.
